[project]
name = "download-ssm"
version = "0.1.0"
description = "GDC mutation data downloader for TCGA-BRCA"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31.0",
    "pandas>=2.0.0",
    "tqdm>=4.65.0",
    "urllib3>=2.0.0",
]
